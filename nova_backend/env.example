# ── Azure AD App Registration ─────────────────────────────────────────────────
# Set AZURE_TENANT_ID=placeholder to run without Azure AD (dev bypass active).
# Set real values to enable JWT validation on all protected routes.
AZURE_TENANT_ID=placeholder
AZURE_CLIENT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
AZURE_CLIENT_SECRET=your-client-secret-here
AZURE_AUTHORITY=https://login.microsoftonline.com/placeholder

# ── Azure OpenAI ──────────────────────────────────────────────────────────────
# OPENAI_MODEL must match the deployment name in your Azure OpenAI resource,
# not the model name (they may differ, e.g. deployment "nova-gpt4o" → model gpt-4o-mini).
OPENAI_API_KEY=your-azure-openai-key-here
OPENAI_MODEL=gpt-4o-mini
OPENAI_API_VERSION=2025-01-01-preview
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/

# ── Classmate table-dump API (replaces Fabric SQL) ────────────────────────────
# Nightly sync pulls whole tables into a local SQLite warehouse (nova_warehouse.db).
API_ENDPOINT_URL=https://your-apim-host.azure-api.net/path/to/invoke
API_SUBSCRIPTION_KEY=your-apim-subscription-key-here
API_TENANT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
API_CLIENT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
API_CLIENT_SECRET=your-api-client-secret-here
API_SCOPE=api://xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx/.default

# ── Nova App ──────────────────────────────────────────────────────────────────
NOVA_ENV=development
NOVA_SECRET_KEY=generate-a-secure-random-string-here
NOVA_CORS_ORIGINS=["http://localhost:5500","http://127.0.0.1:5500","http://localhost:8000","http://127.0.0.1:8000"]
# Set NOVA_DEV_BYPASS=false in production (never trust dev identity headers there).
NOVA_DEV_BYPASS=false

# ── Authorization allowlists (kept out of source; set real IDs here) ──────────
# Executive users who may view the company-wide Overview / run exec people search.
EXEC_USER_IDS=[]
# Executive users allowed to use the dev "view as" impersonation path.
EXEC_DEV_USER_IDS=[]
# Dev-bypass fallback identity (only used when NOVA_DEV_BYPASS=true).
DEV_FALLBACK_USER_ID=0
DEV_FALLBACK_EMAIL=

# ── Databases (put on persistent, writable storage in production) ──────────────
# In Azure App Service use the persistent /home mount so a code deploy never
# wipes the seeded/scored data. Seed both files once, then set these paths.
# Leave unset for local dev (defaults to nova_backend/nova_*.db).
#   WAREHOUSE_DB_PATH=/home/data/nova_warehouse.db
#   NOVA_LOCAL_DB_PATH=/home/data/nova_local.db

# Backstop: set to false in production once nova_local.db is seeded so the app
# NEVER runs the ~8h full-catalogue GPT rescore (even if the DB is missing).
# Re-enable only for a deliberate maintenance run to score newly-added courses.
NOVA_COURSE_SCORING_ENABLED=true
